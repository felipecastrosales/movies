name: movies
description: Movie App.
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  cached_network_image: ^3.4.0
  dio: ^5.5.0+1
  flutter:
    sdk: flutter
  lottie: ^3.1.2
  shared_preferences: ^2.3.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/lottie.json
